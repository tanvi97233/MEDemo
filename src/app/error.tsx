"use client";import{AlertTriangle}from"lucide-react";import{button}from"@/components/ui";export default function ErrorPage({reset}:{error:Error&{digest?:string};reset:()=>void}){return <div className="mx-auto max-w-lg rounded-2xl border border-red-200 bg-white p-8 text-center"><AlertTriangle className="mx-auto text-red-500" size={32}/><h1 className="mt-4 text-xl font-bold">We couldn’t load this view</h1><p className="mt-2 text-sm text-slate-600">Your data is unchanged. Retry the request, or return through the main navigation.</p><button className={`${button} mt-5`} onClick={reset}>Try again</button></div>}
